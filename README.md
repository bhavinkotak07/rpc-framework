Build a RPC framework using JAVA that takes an interface and creates a Stub on client side. 
A RPC server handles concurrent requests from client unmarshals the request data and execute the procedure on the server, create a JSON based response and sends it to client over TCP channel.
